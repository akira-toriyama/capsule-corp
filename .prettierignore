# ツール／スクリプト生成・管理ファイル（prettier で整形すると生成元と
# 食い違い CI churn の原因になるため対象外）。
# ※ gitignore 構文: 行末コメント不可。コメントは行頭 # のみ。

# scripts/gen-eiji-drawer-map.py が厳密一致で生成
keymap_drawer.config.yaml

# caksoylar/keymap-drawer CI が生成・コミット
keymap-drawer/imprint.yaml

# keymap エディタ等のツール管理データ
config/imprint.json

# vanilla west / Docker 由来の生成物
firmware/
